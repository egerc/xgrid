# xgrid

## Quick start

1) Create a config:

```
cp config.example.json config.json
```

2) Run the showcase:

```
python showcase.py --output results.csv
```

If you want to import `xgrid` from other scripts without modifying `sys.path`, install in editable mode:

```
pip install -e .
```
